config:
  target: "https://projetousa.uc.r.appspot.com/"
  phases:
    - duration: 60  # Teste de 60 segundos
      arrivalRate: 10  # 10 utilizadores por segundo
scenarios:
  - flow:
      - get:
          url: "/"